PropOps Copilot - AI Work Breakdown
===================================

Runtime boundary
----------------

- Angular frontend talks only to the .NET API
- .NET API owns authentication, validation, persistence, and frontend-facing contracts
- All AI-specific work must run in the separate Python AI service
- The .NET API should call the Python service whenever AI behavior is needed

Level 1 - Intake and normalization
----------------------------------

1. Add non-portal intake connectors
   - email ingestion
   - SMS/chat ingestion
   - phone-note/manual entry ingestion
   - unify all channels into one request format

2. Build preprocessing pipeline
   - normalize messy text
   - attach property/unit/tenant metadata
   - detect after-hours context
   - standardize payloads before AI

Level 1 note:
- deterministic intake normalization can remain in the .NET application layer
- anything model-driven or AI-assisted should move to the Python service boundary

Level 2 - Rules and knowledge
-----------------------------

3. Implement retrieval/business-rules layer
   - maintenance SOPs
   - vendor rules
   - emergency policy
   - property-specific notes
   - keep this outside the model

4. Define AI input/output contracts
   - input schema for triage requests
   - output schema for:
      - category
      - priority
     - vendor type
      - dispatch decision
      - internal summary
      - tenant response draft

Level 2 note:
- rules and knowledge now live in the Python AI service as structured data, not in the model
- the .NET API exposes triage contract and knowledge-preparation endpoints for staff workflows

Level 3 - First AI path
-----------------------

5. Add baseline inference
   - use a strong instruct model first
   - integrate behind an application service
   - validate the end-to-end path before fine-tuning

6. Add validation and guardrails
   - JSON/schema validation
   - policy checks
   - emergency keyword checks
   - confidence/fallback rules

Level 3 note:
- baseline inference now runs in the Python AI service through a LangGraph workflow behind a configurable provider adapter
- the default local mode is a heuristic baseline so the end-to-end path stays testable without an external model host
- the .NET API exposes triage inference results with guardrail metadata, confidence, and human-review fallback signals

Level 4 - Human-in-the-loop operations
--------------------------------------

7. Expand human review workflow
   - allow staff to approve/edit AI outputs [implemented]
   - override category/priority/dispatch decision [implemented]
   - edit tenant response draft [implemented]

8. Build action integrations
   - create work order [implemented as simulated action log]
   - assign vendor [implemented as simulated action log]
   - notify tenant [implemented as simulated action log]
   - log internal note [implemented]

Level 4 note:
- staff-facing operations detail UI now runs Level 3 inference, lets staff edit/approve the output, and saves an auditable triage review
- the .NET API persists triage reviews and operational action events in PostgreSQL
- external work-order, vendor, SMS, and email providers are still simulated by action log records

Level 5 - Learning loop
-----------------------

9. Capture feedback data
   - final resolution [implemented]
   - corrected priority/category [implemented]
   - edited response draft [implemented as final tenant response]
   - dispatch outcome [implemented]

10. Prepare fine-tuning dataset
    - convert reviewed cases into training examples [implemented as stable candidates]
    - clean labels [implemented through corrected category/priority]
    - stable structured outputs [implemented as JSON snapshots]
    - remove noisy/low-quality examples [implemented through exclusion rules]

Level 5 note:
- feedback capture lives in the .NET/API and Angular operations workflow, not in the live Python inference path
- reviewed and resolved cases create fine-tuning candidate snapshots for later export
- noisy, duplicate, cancelled, non-maintenance, or explicitly excluded cases are marked excluded from training
- future synthetic data generation should add offline Python scripts and scenario libraries to accelerate dataset volume:
   - `synthetic_maintenance_scenarios.json`
   - `generate_synthetic_dataset.py`
   - `validate_dataset.py`
   - separate synthetic training candidates from synthetic eval/guardrail cases

Level 6 - Model specialization
------------------------------

11. Fine-tune the triage model
    - tune for property-management triage behavior
    - tune for structured output consistency
    - tune for tenant response tone/style

12. Serve model with vLLM
    - deploy selected/fine-tuned model behind vLLM
    - expose application-facing API
    - make app call the model through a stable service layer
   These are the steps don't to enable serving model from vLLM not ollama.
   ``` Step 1
        export HF_MODEL_HANDLE=Qwen/Qwen2.5-3B-Instruct

    Step 2
        export LATEST_VLLM_VERSION=26.02-py3
        docker pull nvcr.io/nvidia/vllm:${LATEST_VLLM_VERSION}

    Step 3:

        docker run --rm -it --gpus all \
        -p 8001:8000 \
        nvcr.io/nvidia/vllm:${LATEST_VLLM_VERSION} \
        vllm serve ${HF_MODEL_HANDLE}

    Step 4: Verify
        curl -s http://localhost:8001/v1/chat/completions   -H "Content-Type: application/json"   -d '{
            "model": "Qwen/Qwen2.5-3B-Instruct",
            "messages": [
            { "role": "user", "content": "Reply with only the number: 12 * 17" }
            ],
            "max_tokens": 20
        }' | jq '.choices[0].message.content' ```

   Current Level 6 checkpoint:
     Angular -> .NET API -> Python AI service -> vLLM -> Qwen model.
   - A schema issue was observed and fixed through prompt tightening: the model returned `confidence_score` as 85 instead of 0.85.
   - 20 seed maintenance cases have resolution feedback saved.
   - All 20 seed cases now have staff triage review and feedback.
   - Dataset candidates currently include 24 usable Candidate rows and 4 older Excluded rows from before review backfill.
   - Level 5 candidates were exported and converted to chat-format JSONL with:
     `python3 scripts/export_level6_finetune_dataset.py`
   - look for explanation.txt for more explanation on files 
   - Current fine-tuning split: 21 deduplicated usable examples, 16 train rows, 5 eval rows.
   - Baseline eval script:
     `python3 scripts/run_level6_eval.py`
   - Baseline result file:
     `data/level6-finetuning/baseline-eval-results.json`
   - Baseline Qwen/Qwen2.5-3B-Instruct eval result:
     - schema valid: 5/5
     - category match: 5/5
     - priority match: 3/5
   - Step 3B fine-tuning decision:
     - target model: `Qwen/Qwen2.5-3B-Instruct`
     - first training method: Hugging Face + TRL + PEFT QLoRA
     - train file: `data/level6-finetuning/train.chat.jsonl`
     - eval file: `data/level6-finetuning/eval.chat.jsonl`
     - output adapter directory: `models/propops-qwen2.5-3b-lora`
   - Why Hugging Face/TRL/PEFT first instead of NVIDIA/NeMo-style fine-tuning:
     - it is the most common beginner-friendly LoRA/QLoRA path and maps directly to many tutorials
     - it produces a standard PEFT adapter that can be inspected, merged, or served later
     - it keeps the first experiment small and framework-portable
     - it matches the learning goal: understand adapter fine-tuning mechanics before adopting a heavier platform workflow
     - NVIDIA/NeMo-style training may fit DGX Spark well later, but it is more platform-specific and adds extra concepts before the basic loop is proven
   - Step 3C tiny QLoRA training checklist:
     - 3C.1 Create an isolated fine-tuning Python environment:
       `python3 -m venv .venv-finetune`
       `source .venv-finetune/bin/activate`
       `python -m pip install --upgrade pip`
     - 3C.2 Install the training stack:
       `pip install torch transformers datasets peft trl accelerate bitsandbytes`
     - 3C.3 Create a small training script that reads:
       `data/level6-finetuning/train.chat.jsonl`
       and writes adapters to:
       `models/propops-qwen2.5-3b-lora`
     - 3C.4 Run a tiny smoke training job first, using very small settings so the pipeline is proven before longer experiments.
     - 3C.5 Confirm adapter files were created, especially:
       `adapter_config.json`
       `adapter_model.safetensors`
   - Step 3C completion checkpoint:
     - `.venv-finetune` was created locally and should not be committed.
     - Training stack installed successfully: torch, transformers, datasets, peft, trl, accelerate, bitsandbytes.
     - PyTorch sees the NVIDIA GB10 through CUDA with about 121.69 GB available.
     - QLoRA 4-bit loading was very slow on this stack, so the first successful adapter run used regular LoRA with `--no-qlora`.
     - Smoke adapter created at:
       `models/propops-qwen2.5-3b-lora-smoke`
     - First real tiny adapter created at:
       `models/propops-qwen2.5-3b-lora`
     - Confirmed adapter files:
       `models/propops-qwen2.5-3b-lora/adapter_config.json`
       `models/propops-qwen2.5-3b-lora/adapter_model.safetensors`
     - Model artifacts under `models/` are generated local outputs and should not be committed.
   - Step 3D comparison checkpoint:
     - LoRA eval result file:
       `data/level6-finetuning/lora-eval-results.json`
     - Base model result:
       - schema valid: 5/5
       - category match: 5/5
       - priority match: 3/5
     - LoRA adapter result:
       - schema valid: 5/5
       - category match: 5/5
       - priority match: 3/5
     - The first 20-step adapter proved the fine-tuning/eval loop but did not improve the tiny holdout metric.
     - The likely causes are tiny dataset size, only 16 training rows, and a very short 20-step adapter run.
   - Next resume step: either improve dataset/training settings for another adapter run, or move to Step 3E and learn how to serve the current adapter through vLLM.


Level 7 - Quality and safety
----------------------------

13. Evaluate continuously
    - triage accuracy
    - emergency detection quality
    - schema compliance
    - policy adherence
    - automation vs human-review rate

Recommended implementation order
--------------------------------

1. Intake connectors
2. Preprocessing
3. Retrieval/business rules
4. Output contracts
5. Baseline inference
6. Guardrails
7. Human review
8. Action integrations
9. Feedback capture
10. Fine-tuning dataset
11. Fine-tuning
12. vLLM serving
13. Evaluation

Important design rule
---------------------

- RAG/rules should hold changing knowledge
- Fine-tuning should shape behavior and output style
- vLLM should handle serving, not training

Immediate next milestone
------------------------

Synthetic dataset generation scripts and a curated scenario library for solo-development training volume
